<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
    <nav class="homenav">
  <ul class="nav nav-tabs">

    <li><a href="/playlist">HOME</a></li>
    <li><a href="/songs">SONGS</a></li>

  </ul>
</nav>
<div class="navbar">
  <div class="edit">


  <a  href="/playlist/<%=onePlaylist.id%>/edit"><button type="button" name="button" class="edit-button">Edit Playlist</button></a>
  </div>
  <form action="/playlist/<%=onePlaylist._id%>?_method=DELETE" method="POST">
          <button class="delete-button">Delete Playlist</button>
        </form>
</div>


<div class="image">

    <img src='<%= onePlaylist.url %>' height="400" width="600" />



<div class="addsong">
    <form action='/songs' method='post'>
      <h5 class="text">Add a song!</h5>
      <input type='text' name='artist' placeholder='Artist' />
      <input type='text' name='song' placeholder='Song' />
      <input type='hidden' name='playlist' value='<%= onePlaylist._id %>' />
      <input type='length' name='length' placeholder='Length' />
      <input type='text' name='written_by' placeholder='Writer' />
      <input type='text' name='producer' placeholder='Producer' />
      <button type='submit'>Add Song</button>
      <a href="/playlist"><button type="button" name="button">Back</a></button>
</div>
</div>

<div class="playsub">
     <div class="submit text">
<p>Playlist Name: <%= onePlaylist.playlistName %> </p>
<p>Submitted by: <%= onePlaylist.submitted_by %> </p>

    </div>



<div class="playtable">

    <table class="table table-striped">

    <thead>
    <tr>



    <th class="text">Artist</th>
     <th class="text">Song</th>
     <th class="text">Length</th>
      <th class="text">Writer</th>
       <th class="text">Producer</th>

     </tr>
     </thead>

     <div class="media">
     <tbody>
       <% for (let i = 0; i < songs.length; i++) { %>
         <tr>
      <td><a href="/songs/<%= songs[i].id %>/"><%= songs[i].artist %></td>
      <td><a href="/songs/<%= songs[i].id %>/"><%= songs[i].song %></td>
      <td><a href="/songs/<%= songs[i].id %>/"><%= songs[i].length %></td>
      <td><a href="/songs/<%= songs[i].id %>/"><%= songs[i].written_by %></td>
      <td><a href="/songs/<%= songs[i].id %>/"><%= songs[i].producer %></td>
    <% } %>
  </tbody>
 </div>
  </table>
</div>
</div>


  </body>
</html>
